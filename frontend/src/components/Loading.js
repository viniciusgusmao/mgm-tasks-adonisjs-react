import React from 'react';
import Loading from 'react-fullscreen-loading';

const Loading_ = ({loading}) => <Loading loading={loading} background="transparent" loaderColor="#EE6506" />;

export default Loading_;